import { render } from '@testing-library/react'

const Component = () => {
  return <div>gest</div>
}

describe('AuthContextProvider', () => {
  it('', () => {
    render(<Component />)
    expect(true).toBe(true)
  })
})
